{
  "name": "single-price-grid-component",
  "version": "1.0.0",
  "description": "3-column-component challenge",
  "main": "tailwind.config.js",
  "scripts": {
    "start_http": "concurrently --kill-others \"tailwindcss -i ./src/input.css -o ./dist/output.css --watch\" \"http-server src\"",
    "start": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.1.0",
    "prettier-plugin-tailwindcss": "^0.5.7",
    "tailwindcss": "^3.3.6"
  }
}
