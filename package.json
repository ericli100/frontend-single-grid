{
  "name": "single-price-grid-component",
  "version": "1.0.0",
  "description": "3-column-component challenge",
  "main": "tailwind.config.js",
  "scripts": {
    "start": "concurrently --kill-others \"tailwindcss -i ./src/input.css -o ./src/dist/output.css --watch\" \"live-server src\"",
    "tailwind": "tailwindcss -i ./src/input.css -o ./src/dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.1.0",
    "prettier-plugin-tailwindcss": "^0.5.7",
    "tailwindcss": "^3.3.6"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2"
  }
}
